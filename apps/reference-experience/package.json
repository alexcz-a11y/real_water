{
  "name": "@real-water/reference-experience",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "@tweakpane/core": "2.0.5",
    "real-water": "workspace:*",
    "tweakpane": "4.0.5"
  }
}
